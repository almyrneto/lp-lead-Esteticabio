import { styled } from "styled-components";
import { colors } from "../../theme";


export const ButtonRegisterStyle = styled.button`
    width: 249px;
    height: 54px;
    border-radius: 16px;
    border: none;
    background: ${colors.verdePrincipal};
    color: ${colors.branco};
    font-weight: 600;
    font-size: 16px;
    cursor: pointer;
`
